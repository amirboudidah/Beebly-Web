{% extends 'base.html.twig' %}

{% block title %}ClotureAchat{% endblock %}

{% block body %}
    <h1>ClotureAchat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCloture</th>
                <td>{{ cloture_achat.idCloture }}</td>
            </tr>
            <tr>
                <th>Pointachatfinale</th>
                <td>{{ cloture_achat.pointachatfinale }}</td>
            </tr>
            <tr>
                <th>Etatcloture</th>
                <td>{{ cloture_achat.etatcloture }}</td>
            </tr>
            <tr>
                <th>Etatlivre</th>
                <td>{{ cloture_achat.etatlivre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cloture_achat_index') }}">back to list</a>

    <a href="{{ path('app_cloture_achat_edit', {'idCloture': cloture_achat.idCloture}) }}">edit</a>

    {{ include('cloture_achat/_delete_form.html.twig') }}
{% endblock %}
